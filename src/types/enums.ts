// /types/enums.d.ts
// Contains reusable enum or union types that are used across the application.


export type PaymentMethod = 'CASH' | 'CARD';

export type RoleName = 'ADMIN' | 'MANAGER' | 'AUDITOR';

